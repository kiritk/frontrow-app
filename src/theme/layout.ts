// Floating tab-bar pill dimensions. Single source of truth so screens can
// compute their own bottom content insets — the pill is absolutely positioned,
// so nothing else reserves space for it automatically.
export const TAB_BAR_HEIGHT = 60;
export const TAB_BAR_BOTTOM_OFFSET = 24;
